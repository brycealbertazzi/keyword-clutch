{
  "type": "module",
  "scripts": {
    "client-side": "cd client-side && npm start",
    "server-side": "node server-side/server.js",
    "dev": "concurrently \"PORT=56897 npm run client-side\" \"PORT=45890 npm run server-side\"",
    "heroku-postbuild": "cd client-side && npm install && npm run build"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.0.1",
    "apify": "^3.1.15",
    "cheerio": "^1.0.0-rc.12",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dot-env": "^0.0.1",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "google-ads-api": "^14.2.0",
    "google-ads-node": "^12.0.2",
    "googleapis": "^133.0.0",
    "http-proxy-middleware": "^2.0.6",
    "readline": "^1.3.0"
  }
}
